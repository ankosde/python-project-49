import prompt
from random import randint
from brain_games.games.brain_even_games import essence_of_the_game
from brain_games.games.brain_even_games import random_number
from brain_games.games.brain_even_games import condition_check

def running_game():
    print('Welcome to the Brain Games!')
    name = prompt.string('May I have your name? ')
    print(f'Hello, {name}!')
    essence_of_the_game()
    count = 0
    while count < 3:
       final = f'Congratulations, {name}!'
       random_num = random_number()
       print(f'Question: {random_num}')
       answer = prompt.string('Your answer: ')
       print(answer) 
       if condition_check(random_num, answer) == 'correct':
          print('Correct!')
          count +=1
       else:
          correct_answer =  poetry run brain-even
          print(f'{answer} is wrong answer ;(. Correct answer was {condition_check}.')
          print(f'''Let's try again, {name}!''')
          count = 0
    print(final)
